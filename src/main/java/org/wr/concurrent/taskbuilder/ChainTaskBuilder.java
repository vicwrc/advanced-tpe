package org.wr.concurrent.taskbuilder;

import org.wr.concurrent.TaskBuilder;
import org.wr.concurrent.TaskQueue;

public class ChainTaskBuilder implements TaskBuilder {
    @Override
    public TaskQueue build() {
        return null;
    }
}
